
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

// TEMPORARY SOLUTION:
// We're using direct Supabase access as a hotfix while the BLUE API (api.blue.legal) is unavailable.
// This approach maintains the same interface for components but changes the implementation.
// The long-term plan is to revert to using the API once it's available again.
// An issue should be created to track this temporary solution and plan the transition back.

const SUPABASE_URL = "https://zcwtzaebusnoqzbfxyqf.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inpjd3R6YWVidXNub3F6YmZ4eXFmIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDY0NzQ0NzUsImV4cCI6MjA2MjA1MDQ3NX0.cB0P6j1fyreAFTnoaFsygvJ_hzx41Nsp6Wb-SATbtqs";
const SUPABASE_SERVICE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inpjd3R6YWVidXNub3F6YmZ4eXFmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc0NjQ3NDQ3NSwiZXhwIjoyMDYyMDUwNDc1fQ.PArna2NYmnPdZmeLsHEAgvvAR207WjZOg0T2qfeb1U8";

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    storage: localStorage,
    autoRefreshToken: true,
    persistSession: true,
    detectSessionInUrl: false,
  },
  realtime: {
    params: {
      eventsPerSecond: 10,
    },
  },
  global: {
    fetch: (input, init) => {
      // Add custom fetch logic for better request error handling
      return fetch(input, init).catch(err => {
        console.error('Network error during Supabase request:', err);
        throw err;
      });
    },
  },
});

// Add a convenience method to check if we have a valid session
export const hasValidSession = async (): Promise<boolean> => {
  try {
    const { data, error } = await supabase.auth.getSession();
    return !error && !!data.session;
  } catch (err) {
    console.error('Error checking session:', err);
    return false;
  }
};
